parameters:
    level: 8
    phpVersion: 80400
    paths:
        - src
    excludePaths:
        - var
        - vendor
    # Ignorer les faux positifs connus lors des premières passes de migration
    # (décommenter au fur et à mesure que le code est corrigé)
    # ignoreErrors:
    #     - '#Call to an undefined method#'
