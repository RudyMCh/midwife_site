<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0">

        <title>{% block title %}{{ meta_title is defined and meta_title ? meta_title : 'Sages-femmes Quetigny' }}{% endblock %}</title>

        <meta name="description" content="{% block meta_description %}{{ meta_description is defined and meta_description ? meta_description : '' }}{% endblock %}">
        <meta name="robots" content="{% block robots %}INDEX, FOLLOW{% endblock %}">

        {% block canonical %}
            <link rel="canonical" href="{{ app.request.schemeAndHttpHost ~ app.request.pathInfo }}">
        {% endblock %}

        {# Open Graph #}
        <meta property="og:type" content="{% block og_type %}website{% endblock %}">
        <meta property="og:locale" content="fr_FR">
        <meta property="og:title" content="{% block og_title %}{{ meta_title is defined and meta_title ? meta_title : 'Sages-femmes Quetigny' }}{% endblock %}">
        <meta property="og:description" content="{% block og_description %}{{ meta_description is defined and meta_description ? meta_description : '' }}{% endblock %}">
        <meta property="og:url" content="{{ app.request.uri }}">
        {% block og_image %}
            <meta property="og:image" content="{{ absolute_url(asset('favicon/apple-touch-icon.png')) }}">
            <meta property="og:image:width" content="180">
            <meta property="og:image:height" content="180">
        {% endblock %}

        {# Twitter Cards #}
        <meta name="twitter:card" content="summary_large_image">
        {% block twitter_image %}
            <meta name="twitter:image" content="{{ absolute_url(asset('favicon/apple-touch-icon.png')) }}">
        {% endblock %}

        {# Favicons #}
        <link rel="shortcut icon" type="image/png" href="{{ asset('favicon/favicon.ico') }}"/>
        <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('favicon/apple-touch-icon.png') }}">
        <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('favicon/favicon-32x32.png') }}">
        <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('favicon/favicon-16x16.png') }}">

        <link rel="stylesheet" href="{{ asset('styles/front.scss') }}">
        {% block stylesheets %}{% endblock %}

        {% block json_ld %}{% endblock %}

        {# Matomo cookieless — configurer MATOMO_URL et MATOMO_SITE_ID dans .env.local #}
        {% if app.environment == 'prod' and matomo_url is defined and matomo_url %}
        <script>
            var _paq = window._paq = window._paq || [];
            _paq.push(['disableCookies']);
            _paq.push(['trackPageView']);
            _paq.push(['enableLinkTracking']);
            (function() {
                var u = {{ matomo_url|json_encode|raw }};
                _paq.push(['setTrackerUrl', u + 'matomo.php']);
                _paq.push(['setSiteId', {{ matomo_site_id|default(1) }}]);
                var d = document, g = d.createElement('script'), s = d.getElementsByTagName('script')[0];
                g.async = true; g.src = u + 'matomo.js'; s.parentNode.insertBefore(g, s);
            })();
        </script>
        {% endif %}

        {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% block javascripts %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
